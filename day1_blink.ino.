// E&C Day 1: The Digital Output
// Controlling voltage on Pin 13

int ledPin = 13; 

void setup() {
  pinMode(ledPin, OUTPUT); 
}

void loop() {
  digitalWrite(ledPin, HIGH); // Turn LED ON (5V)
  delay(1000);                // Wait 1 second
  digitalWrite(ledPin, LOW);  // Turn LED OFF (0V)
  delay(1000);                // Wait 1 second
}
